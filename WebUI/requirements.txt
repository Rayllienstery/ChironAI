# Legacy convenience list. Primary dependency declarations live in the repository root
# pyproject.toml (`pip install -e .` from project root). Use that for a consistent venv.

# RAG pipeline + crawl
flask
requests
qdrant-client
langchain-text-splitters
lxml>=6.0.0; platform_system != "Windows"
lxml>=6.0.0; platform_system == "Windows" and python_version >= "3.11"

# Crawler: Playwright
playwright

# Better code-block preservation in HTML→markdown (recommended for Apple docs)
html2text
