[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "chironai-extensions-sandbox"
version = "0.1.0"
description = "Out-of-process extension sandbox for ChironAI"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["extensions_sandbox"]
