[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "chironai-security"
version = "0.1.0"
description = "Security audit helpers for ChironAI CoreModules"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["chironai_security"]
