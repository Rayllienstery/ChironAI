# chironai-html-md

Deterministic HTML → Markdown helpers (regex fallback, lxml DOM path, optional html2text for code preservation).

Install from repo root:

```bash
pip install -e Core/modules/html_md
```

Used by `crawler_service` and `external_docs_rag`.
