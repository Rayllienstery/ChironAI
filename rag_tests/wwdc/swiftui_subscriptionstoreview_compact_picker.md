# SwiftUI SubscriptionStoreView compact picker style

Platform: iOS
Framework: SwiftUI
MinOS: iOS 18
Difficulty: intermediate
Rag Required: no
Concept Mode: all
RAG Strict: true

## Question

In WWDC24 Apple introduced new control styles for `SubscriptionStoreView`.  
Explain how to use the **compactPicker** control style in a SwiftUI subscription store:

- what layout it produces and when it is appropriate to use it;
- how it differs from the default (vertical) styles and from `pagedPicker` / `pagedProminentPicker`;
- how to combine `compactPicker` with the `subscriptionStoreControlStyle` modifier and `placement` (for example, moving controls into the bottom bar);
- recommended scenarios and limitations (e.g. number of plans, screen space, marketing content).

Provide a short SwiftUI code example that configures a `SubscriptionStoreView` with the compact picker style for a typical subscription group.

## Expected Concepts

- SubscriptionStoreView
- compactPicker
- subscriptionStoreControlStyle
- control placement
- subscription options
- marketing content

## RAG Requirement

The answer should be grounded in the WWDC24 session about the subscription store view and its new control styles, not only in generic SwiftUI documentation.

## Notes

This test is intended to trigger WWDC24 content about SubscriptionStoreView control styles (compact picker) from the `wwdc_sessions_2019_plus` source.*** End Patch```}/>
