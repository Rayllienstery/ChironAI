# SwiftUI SubscriptionStoreView paged picker styles

Platform: iOS
Framework: SwiftUI
MinOS: iOS 18
Difficulty: intermediate
Rag Required: no
Concept Mode: all
RAG Strict: true

## Question

WWDC24 introduces two new paged styles for `SubscriptionStoreView`: **pagedPicker** and **pagedProminentPicker**.  
Explain:

- how `pagedPicker` and `pagedProminentPicker` lay out subscription options and how they differ from each other;
- when you should prefer a paged style instead of the compact picker or the classic vertical layout;
- how these styles interact with subscription option groups and group sets (for example, grouping by level of service vs period);
- how to configure one of these styles in code, including where you attach the modifier.

Provide a concise SwiftUI example that uses `SubscriptionStoreView` with a paged style to present a small set of subscription plans.

## Expected Concepts

- SubscriptionStoreView
- pagedPicker
- pagedProminentPicker
- subscription option groups
- group set
- subscription store control styles

## RAG Requirement

The answer should use details from the WWDC24 session on SubscriptionStoreView control styles, not only generic documentation.

## Notes

Designed to pull WWDC24 content about the paged picker styles and their recommended use cases.*** End Patch
