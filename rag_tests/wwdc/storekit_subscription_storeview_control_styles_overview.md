# StoreKit SubscriptionStoreView control styles overview

Platform: iOS
Framework: StoreKit
MinOS: iOS 18
Difficulty: intermediate
Rag Required: no
Concept Mode: all
RAG Strict: true

## Question

Summarize the new SubscriptionStoreView control styles introduced in the WWDC24 StoreKit session and explain:

- how the total number of standard control styles changed and why Apple added more;
- when to use each style (default, compactPicker, pagedPicker, pagedProminentPicker) depending on the number of plans and available vertical space;
- how control styles interact with `placement` (e.g. moving controls into a bottom bar on iOS or edges on tvOS);
- any platform-specific notes mentioned in the session (for example, default placements on tvOS).

You may reference SwiftUI in your explanation, but focus on the StoreKit SubscriptionStoreView API as described in WWDC24.

## Expected Concepts

- SubscriptionStoreView
- control styles
- compactPicker
- pagedPicker
- pagedProminentPicker
- placement

## RAG Requirement

The answer should reflect details and recommendations from the WWDC24 StoreKit views session rather than generic StoreKit 2 docs.

## Notes

This test is meant to exercise high-level understanding of the new subscription store control styles from WWDC.*** End Patch
