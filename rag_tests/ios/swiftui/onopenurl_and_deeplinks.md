# Handling deep links with onOpenURL in SwiftUI

Platform: iOS
Framework: SwiftUI
MinOS: iOS 14
Difficulty: intermediate
Concept Mode: all
RAG Strict: false

## Question

You need to support deep links that open specific screens and states in a SwiftUI app.
Explain:

- how to use `onOpenURL` to handle incoming URLs;
- how to parse the URL and map it to navigation state (e.g. NavigationPath);
- how to coordinate deep link handling with existing navigation logic;
- common mistakes (handling links only in one view, not normalizing URLs, ignoring multiple active scenes).

## Expected Concepts

- onOpenURL
- deep link
- URL parsing
- navigation state

## RAG Requirement

The answer should align with SwiftUI and iOS documentation on deep linking and URL handling.

## Notes

Checks that the assistant can reason about deep links in a modern SwiftUI application.

