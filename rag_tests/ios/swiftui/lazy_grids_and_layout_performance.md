# Lazy grids and layout performance in SwiftUI

Platform: iOS
Framework: SwiftUI
MinOS: iOS 15
Difficulty: intermediate
Concept Mode: all
RAG Strict: false

## Question

You display a large collection of items using `LazyVGrid` and `LazyHGrid`.
Explain:

- how lazy grids differ from regular stacks in terms of performance and memory;
- how to configure grid items (fixed, flexible, adaptive) for different screen sizes;
- how to avoid layout thrashing and unnecessary recomputation;
- when to consider custom `Layout` for more complex scenarios.

## Expected Concepts

- LazyVGrid
- LazyHGrid
- GridItem
- performance
- custom Layout

## RAG Requirement

The answer should align with SwiftUI documentation on lazy grids and layout.

## Notes

Checks knowledge of layout and performance tuning in SwiftUI.

