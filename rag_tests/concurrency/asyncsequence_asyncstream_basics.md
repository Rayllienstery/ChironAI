# AsyncSequence and AsyncStream basics

Platform: iOS
Framework: Swift
MinOS: iOS 15
Difficulty: intermediate
Concept Mode: all
RAG Strict: true

## Question

Explain:

- what `AsyncSequence` is and how you can implement it using `AsyncStream`;
- how to use a `for await` loop to consume values;
- how cancellation works when iterating over an `AsyncSequence`;
- how `AsyncSequence` relates to Combine publishers (similarities and differences).

## Expected Concepts

- AsyncSequence
- AsyncStream
- for await
- cancellation
- Combine publisher

## RAG Requirement

The answer must reference Apple documentation for `AsyncSequence` / `AsyncStream`
and, if mentioned, Combine.

## Notes

Verifies understanding of async sequences and their relationship to Combine.

