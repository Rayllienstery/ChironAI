# Generics and where constraints in Swift

Platform: iOS
Framework: Swift
MinOS: iOS 15
Difficulty: intermediate
Concept Mode: all
RAG Strict: false

## Question

You design a reusable function and a protocol with associated types in Swift.
Explain:

- how to use generic type parameters and `where` clauses to constrain implementations;
- how associated types interact with `where` constraints in protocol extensions;
- when to use opaque return types (`some`) and `any` for existential types;
- common pitfalls that lead to overly complex or ambiguous generic signatures.

## Expected Concepts

- generics
- where clause
- associatedtype
- some
- any
- protocol extension

## RAG Requirement

The answer should align with Swift generics and protocols documentation.

## Notes

Tests deeper understanding of Swift generics, associated types and modern type features.

